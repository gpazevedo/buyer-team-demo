[project]
name = "buyer-team-demo"
version = "0.1.0"
requires-python = ">=3.14"

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "test_tenant_app/backend",
    "demo-harness-project/backend",
]
